{
  "name": "sonexa-monorepo",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "install": "npm run install:backend && npm run install:frontend",
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend && npm install"
  },
  "dependencies": {
    "next": "16.2.10"
  }
}
